extends Node

#leaf_ is invisiblize the flying object,
#leaf put an obstacle onscreen to cover the object,and distinguish from leaf_, flying object is still visible once it moves outside the obstacle area.
#you can change leaf shape in leaf's code
#youcan decide to use leaf_ or leaf in level3.gd, which is of the root of this scene
#not sure which one has what kind of effect,
# reward is using the same script as Pon2
#------- 2023/10/13 ----------#
